---
const today = new Date();
---
<footer>
    <div class="footer-content">
        <button id="email-button" class="email-button">
            <div class="icon">
                <svg viewBox="0 0 24 24" width="14" height="14">
                    <path d="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z" fill="currentColor"/>
                </svg>
            </div>
            <span id="button-text">E-Mail Valentin</span>
        </button>
        
        <a href="https://www.linkedin.com/in/valentin-courtaud/" class="linkedin-button" target="_blank" rel="noopener">
            <div class="icon">
                <svg viewBox="0 0 24 24" width="14" height="14">
                    <path d="M19 3a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h14m-.5 15.5v-5.3a3.26 3.26 0 0 0-3.26-3.26c-.85 0-1.84.52-2.32 1.3v-1.11h-2.79v8.37h2.79v-4.93c0-.77.62-1.4 1.39-1.4a1.4 1.4 0 0 1 1.4 1.4v4.93h2.79M6.88 8.56a1.68 1.68 0 0 0 1.68-1.68c0-.93-.75-1.69-1.68-1.69a1.69 1.69 0 0 0-1.69 1.69c0 .93.76 1.68 1.69 1.68m1.39 9.94v-8.37H5.5v8.37h2.77z" fill="currentColor"/>
                </svg>
            </div>
        </a>
        
        <span class="separator">|</span>
        
        <span class="opensource">
            <a href="https://github.com/cassidoo/blahg">This blog is open source</a>!
        </span>
    </div>
</footer>

<style>
    footer {
        padding: 25px;
        text-align: center;
        font-size: 0.8rem;
    }
    
    .footer-content {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 10px;
    }
    
    .email-button {
        display: flex;
        align-items: center;
        padding: 4px 10px;
        background-color: var(--background-color);
        color: var(--text-color);
        border-radius: 6px;
        border: 1px solid var(--text-color);
        text-decoration: none;
        font-size: 0.75rem;
        font-weight: 500;
        cursor: pointer;
        height: 24px;
        white-space: nowrap;
    }
    
    .email-button .icon {
        margin-right: 4px;
        display: flex;
        align-items: center;
    }
    
    .linkedin-button {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 4px;
        background-color: var(--background-color);
        color: var(--text-color);
        border-radius: 6px;
        border: 1px solid var(--text-color);
        text-decoration: none;
        transition: background-color 0.2s, color 0.2s;
        cursor: pointer;
        height: 24px;
        width: 24px;
    }
    
    .linkedin-button:hover {
        background-color: var(--text-color);
        color: var(--background-color);
    }
    
    .separator {
        margin: 0 2px;
    }
    
    .opensource a {
        text-decoration: underline;
        text-decoration-thickness: 0.1ex;
        text-underline-offset: 0.1ex;
    }
    
    @media (max-width: 480px) {
        .footer-content {
            flex-wrap: wrap;
        }
    }
</style>

<script>
    // Function to handle the email button click
    document.addEventListener('DOMContentLoaded', () => {
        const emailButton = document.getElementById('email-button');
        const buttonText = document.getElementById('button-text');
        const emailAddress = 'valentin.courtaud@protonmail.com';
        
        if (emailButton) {
            emailButton.addEventListener('click', async () => {
                // Copy email to clipboard
                try {
                    await navigator.clipboard.writeText(emailAddress);
                    
                    // Change button text to "Copied!" in gray
                    if (buttonText) {
                        const originalText = buttonText.textContent;
                        buttonText.textContent = 'Copied!';
                        buttonText.style.color = '#666'; // Gris similaire à "Here's my most recent posts"
                        
                        // Reset text and color after 2 seconds
                        setTimeout(() => {
                            buttonText.textContent = 'E-Mail Valentin';
                            buttonText.style.color = ''; // Réinitialise à la couleur par défaut
                        }, 2000);
                    }
                } catch (err) {
                    console.error('Failed to copy email: ', err);
                }
            });
        }
    });
</script>